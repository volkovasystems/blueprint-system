"use strict";

const Option = (
	function Option( ){

	}
);

const OptionPrototype = (
	Option.prototype
);

const toString = (
		OptionPrototype
		.toString
	=	(
			function toString( ){

			}
		)
);

module.exports = Option;
