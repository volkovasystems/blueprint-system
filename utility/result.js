"use strict";

const Result = (
	function Result( ){

	}
);

const ResultPrototype = (
	Result.prototype
);

const toString = (
		ResultPrototype
		.toString
	=	(
			function toString( ){

			}
		)
);

module.exports = Result;
