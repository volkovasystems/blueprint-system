"use strict";

const Trigger = (
	function Trigger( ){

	}
);

const TriggerPrototype = (
	Trigger.prototype
);

const toString = (
		TriggerPrototype
		.toString
	=	(
			function toString( ){

			}
		)
);

module.exports = Trigger;
